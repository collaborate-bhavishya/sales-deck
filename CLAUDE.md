# ORALAB Client Deck

ORALAB is an AI creative platform for product images and video across verticals. The site is now a landing hub plus three vertical decks, all sharing one `assets/` folder and served by one AWS Amplify app at `deck.oralab.ai` (deploying from `main`, build spec `amplify.yml`, static, no build step — console build command `mkdir -p dist && cp -r index.html jewellery furniture fashion assets dist/`; `assets/`, `jewellery/`, `furniture/` and `fashion/` must all be copied). GitHub repo: `collaborate-bhavishya/sales-deck`. A push updates the live link. Netlify may also still be connected.

- `deck.oralab.ai/` (root `index.html`) — a small LANDING page: ORALAB logo, "The AI creative platform for brands." heading, and 3 image cards linking to `/jewellery/`, `/fashion/`, `/furniture/`. Card images `assets/home-{jewellery,fashion,furniture}.jpg` (currently clean product stand-ins; swap for the vibrant floating-product hero set when supplied). It is a standalone page, NOT a deck.
- `deck.oralab.ai/jewellery/` (`jewellery/index.html`) — the jewellery deck. This is the CANONICAL DECK SOURCE that `build_furniture.py` and `build_fashion.py` read from; its asset `src`s are root-absolute `/assets/…` (so it loads under `/jewellery/` and the generated decks inherit absolute paths). Edit the jewellery deck here.

> IMPORTANT: `index.html` USED to be the jewellery deck (relative `assets/`); it is now the landing page. The jewellery deck moved to `jewellery/index.html` with absolute `/assets/`. Older notes below that say "index.html" for the jewellery deck now mean `jewellery/index.html`.

> Note: the original CLAUDE.md from the first build session was lost. This file was reconstructed on 2026-08-11 from the deck source and the handoff summary. Items marked ⚠ could not be recovered and need confirming against the original conversation.

## Repo layout

- `index.html` — the landing hub page (see above). `jewellery/index.html` — the jewellery deck (styles, 11 slide sections, carousel JS at the bottom); this is the source the build scripts read.
- `assets/` — shared by all pages: `bangle-*.jpg` etc. (slide 3), client `.mp4` videos, `home-*.jpg` (landing cards), and the furniture/fashion `furn-*`/`fash-*` sets.
- `furniture/index.html` — a furniture-vertical variant of the deck (same ORALAB brand/team/platform, furniture copy). Served by the same Amplify app at `deck.oralab.ai/furniture/` (it lives in a real `furniture/` folder so the trailing-slash URL resolves; its slide-3 asset `src`s are root-absolute `/assets/furn-...` so they load under the `/furniture/` path). Built in two steps: `scratchpad/build_furniture.py` scaffolds it from `jewellery/index.html` (swaps all jewellery copy to furniture, marketplaces to Amazon/Wayfair/IKEA/Houzz/Etsy/Walmart/Overstock/West Elm, and turns every product slot into a placeholder), then `scratchpad/fill_slide3.py` rebuilds slide 3 into the real chair+sofa layout. Re-run BOTH in order to regenerate (running only build_furniture.py reverts slide 3 to placeholders). Slide 3 is a 5-page `.bap` carousel (Sofa, Chair, Bed, TV Unit, and a 2nd Sofa) with the "raw hero + output stack" layout: a rose-framed raw factory photo (`.f3raw`) beside a fanned pile of ORA outputs (`.f3stack`) that opens the `#lightbox` on click (group = raw + 8 outputs, from `img.closest('.f3page')`); assets are `furn-{sofa,bed,tvunit}-raw.jpg` + `-01..08.jpg` (real Chattels & More products from the "chattels and more" zip), plus `furn-chair-*` and `furn-sofa2-*` (the earlier chair renders and the earlier sofa renders, restored/kept alongside the new ones). fill_slide3.py's `new_pages` list controls the page order. The cover `FURN_COLS` wall also uses these real images (all stills, no `.cvpb` video badges). The client-work / video slide IS present in the furniture deck (11 slides, same order as the jewellery deck; client-work is now the 3rd content slide, `slides[3]`, moved to sit right after the product carousel). build_furniture.py replaces the jewellery video prows with 7 furniture brand videos `furn-work-1,2,4,5,6,7,8.mp4` (absolute `/assets/` srcs, so they load under `/furniture/`), 4+3 across the 2 `.sub2` pages, type labels (Product film/Campaign/Ad film/Concept · Interior/Lifestyle/Studio), no brand `.ctag`. (furn-work-3 was removed per request.) The `.sub2`/`cvidSync`/`#vlb` JS is kept intact (paused thumbnails + click-to-fullscreen, see slide 9). `furn-work-2.mp4` is landscape (crops in the portrait tile) and ~12MB (ffmpeg not installed to compress). Slide 8 shows 5 client brands on light logo-chips (`background:#F4F4F1`): SmaartCraaft renders its real logo (`/assets/furn-logo-smaartcraaft.png`, sourced from smaartcraafts.com); Hive, Ammri, Living Concept and Chattels & More stay dark-text wordmarks (generic names / no cleanly-extractable official logo). build_furniture.py's `brands` list is `(name, logo-or-None)` tuples and `brand_card(name, logo)` renders an `<img>` or the name; add a logo file and set its path to promote a wordmark to a logo.
- `fashion/index.html` — a fashion-vertical variant, same pattern as furniture, served at `deck.oralab.ai/fashion/`. Built by `scratchpad/build_fashion.py` (single step) from `jewellery/index.html`: fashion copy, marketplaces to Amazon/Ajio/Myntra/Nykaa/ASOS/Zalando/Shein/Farfetch, client-work video slide removed. It then INSERTS a fashion-only "From sketch to finished product" slide right after slide 3 (11 slides total): a `.fgal` gallery = a rose-framed tech-sketch hero (`.fgsketch`, `object-fit:contain`) + an arrow + a `.fggrid` of 5 finished renders (`.fgo`, cover thumbnails: Product, Studio, Editorial, Lifestyle, Details). All six `.fgal img` open the `#lightbox` cycling the group (sketch + 5). Assets `fash-velvet-{sketch,product,studio,editorial,lifestyle,details}.jpg` (root-absolute `/assets/`). The `.fgal` CSS and the `.fgal img` lightbox wiring live in index.html (inert for the other decks, which have no `.fgal`). Slide 8 shows 3 client brands on light logo-chips: Shaurya Sanadhya (`/assets/fash-logo-shaurya.png`) and Cotton Culture (`/assets/fash-logo-cottonculture.jpg`) render real logos (sourced from their official Shopify sites); Kameez stays a dark-text wordmark (generic name). Same `(name, logo-or-None)` `brands` list + `brand_card(name, logo)` pattern as furniture. Slide 3 is real: a raw mannequin photo + a stack of AI-VIDEO outputs (not still images). `build_fashion.py` overwrites the `.subviews` with 4 `.f3row.f3tall` pages (`FASH` list: Dress 1 video, Printed shirt 5, Striped shirt 3, Leather jacket 1). Each page = a rose-framed raw photo (`/assets/fash-<key>-raw.jpg`) + a `.f3stack` of video poster frames (`.fs` imgs carrying `data-vsrc`/`data-vcap`); the shared `.f3stack` handler (in index.html) sees the `data-vsrc` and opens the `#vlb` player cycling that category's videos instead of the image lightbox. The row has NO `.f3page` class, so clicking the raw just zooms it in `#lightbox`. `.f3tall` makes the tiles 9:16 portrait (fashion-only CSS in index.html; jewellery/furniture keep the square-ish tiles) because garment shots were being cropped. Assets: `fash-<key>-raw.jpg` + `fash-<key>-<n>.mp4` (720p, ffmpeg-compressed) + `fash-<key>-<n>.jpg` (poster). A category with `raw_photo_ready=False` in `FASH` renders an honest dashed "Raw … photo coming" placeholder instead of an img (all four now have real raws). Showcase order in fashion is raw->images, then raw->videos, then sketch->finished: build_fashion.py inserts the raw->images carousel slide BEFORE the "What we do" video slide, and the sketch slide after it. The raw->images and the video slide share the heading "One raw photo becomes a full campaign in minutes."; the video slide carries a small `<p class="sub">Image to video</p>` (added by build_fashion, count=1, before the heading is duplicated) and the raw->images slide has no sub. build_fashion.py also inserts a second showcase slide, "raw product to finished images" — a 4-page carousel (Saree, Suit, Fabric to suit, Shirt) of the f3tall raw-hero + IMAGE-output stack (assets `fash-raw-<cat>.jpg` + `fash-out-<cat>-1..N.jpg`; raw + outputs open the `#lightbox`). This carousel is INDEPENDENT of the `.bap` product carousel: its pages are `.rap` (toggled `.on`, same `display:none`/`.on` CSS as `.bap`/`.sub2`), driven by a self-contained `rawShow()` block in index.html (`#rawPrev`/`#rawNext`/`#rawDots`, guarded by `if(raps.length)` so it is inert in the jewellery/furniture decks, which have no `.rap`). Fashion is now 12 slides. Re-run `build_fashion.py` to regenerate.

## Slide map

> FONT: the whole site is **Nunito** now (Google Fonts), headings and body both. It replaced the earlier Bricolage Grotesque (headings) + Inter (body); `font-family:'Nunito',sans-serif` everywhere, incl. the build scripts' brand wordmarks and `cover_lib.py`.

> CURRENT ORDER (jewellery & furniture, 12 tagged slides + cover = 13): Cover, 01 The problem, 02 What we do (product carousel), 03 Client work (video), 04 Who we work with (client brands), 05 The comparison, 06 Under the hood, 07 More on the platform, 08 Technical tools, 09 Revamp programs, 10 How we work, 11 About ORA, 12 Start small. The old "07 Where can you use us" (One platform / Social Media / Website / Marketplaces `.ucard` grid) was REMOVED (its channels story now lives inside slide 07's Integrations + Marketplaces bands). Two global VALUE-ADD slides were added: 07 More on the platform and 08 Technical tools (see below); the closing slides renumbered accordingly. Fashion is the same plus its two extra product slides (raw->images `.rap` carousel + "sketch to finished" `.fgal`) and minus nothing here (14 sections total). The counter and dots are dynamic (`slides.length`), so adding/removing slides self-updates. The per-slide notes below are by CONTENT and keep their original headings; the visible `.tag` numbers are kept ascending on each reorder.
>
> RECENT CONTENT CHANGES (this batch):
> - **06 Under the hood** rewritten: headline "One step for you, five steps for us." + sub "You upload one product image. The ORA Agent handles the five steps below." The five `.stage` boxes are verb-first with 2-line headings, equal height (`.stage .body{flex:1;...}` + `.stage h3{min-height:2.4em}` so icons/headings/descriptions align across all five), enlarged icons/padding: Read Brand Memory, Inspect Product using AI (ORA vision model), Write Structured Prompt (`>_` icon), Generate Images & Videos, Quality Check after Generation.
> - **07 More on the platform** (was slide 08): headline "Every tool you need, in one platform." (1 line, `max-width:none`, no sub). Three bands in a `.grow` flex column: (1) "AI editing" label + 5 loose tiles (Background removal, Background extend, Resize & recrop, Upscale, Text on image); (2) "Integrations" feature-box ("One click to upload your pictures." + Instagram/Shopify/Website pills, brand glyphs reused from the old slide-7 markup); (3) "Marketplaces" feature-box ("Marketplace-ready in one click." + dimensions/lookbook/captions copy + `.lgrid.m4` `.lgt` name chips). The marketplace chips reuse `<div class="lgt"><span>NAME</span></div>` so the build scripts' global name swaps (Flipkart->Wayfair/Ajio etc.) still apply per vertical.
> - **08 Technical tools** (new): headline "Technical tools, built for your ease." + sub, three gold (`#E3B457`) cards: Smart naming, Multiple file formats, Cloud storage.
> - **09 Revamp programs**: Amazon Revamp + Website Revamp `.icard`s (unchanged, just renumbered).

The sub-sections below describe each slide's content (not its current position):

1. Cover — "The AI creative platform for studio-quality product images and video." Redesigned as a moving media wall: `.cvwall` = 5 vertical `.cvcol` columns of `.cvtile` product/video tiles scrolling up/down (`cvup`/`cvdown` keyframes, alternating per column), a radial `.cvscrim`, and a frosted tinted `.cvpanel` (logo + headline) centred on top, with the two positioning-tag badges (`.cvtags`) pinned at the bottom. Rose `.cvpb` ▶ badges mark video tiles. Built by `scratchpad/cover_lib.py` (`wall_cover()` + `cover_css()`); the jewellery deck uses `JEWEL_COLS` with relative `assets/` srcs, furniture uses `FURN_COLS` and fashion uses `FASH_COLS`, both with absolute `/assets/`. (All three decks now use the media wall; `simple_cover()` is unused.) The cover CSS uses `.slide.cover .cvwall/.cvscrim/.cvpanel{position:...!important}` to beat the deck's `.slide>*:not(.blob){position:relative;z-index:1}` rule.
2. Traditional production can be beautiful, but slow, expensive and tedious. (sub bridges into the 5 problem icon-cards; was "Great products deserve better visual velocity.") There is no `.sub` and no photo caption on this slide (both removed for a cleaner look). The left `.card` is just a `.mini` "The traditional way" label over a real "traditional shoot" photo that fills the card (replacing the old inline SVG illustration); the right column is a 2x2 grid of 4 problem `.icard`s. The photo box is `flex:1;min-height:clamp(130px,18vh,180px);position:relative` and the `<img>` inside is `position:absolute;inset:0;width:100%;height:100%;object-fit:cover` — the absolute positioning is REQUIRED: with a normal in-flow `height:100%` img, the image's intrinsic height feeds the grid-cell sizing and balloons the whole row past `.grow` (headline overlap). Absolute-positioning takes the img out of flow so the box (and thus the left card) matches the right icon grid's height exactly and crops via cover. Photo is `assets/shoot-jewellery.jpg` (jewellery), swapped by the build scripts to root-absolute `/assets/shoot-furniture.jpg` and `/assets/shoot-fashion.jpg` for the subfolder decks.
3. With ORA AI, one raw photo becomes a full campaign in minutes. (headline; standard size, wraps to ~3 lines; standard padding, chevrons sit in the `.subwrap` flex row; no footnote) — 4-page `.bap` sub-carousel using the SAME "raw hero + output stack" layout as the furniture deck (`.f3row.f3page`): a rose-framed raw client photo (`.f3raw`, with a "Raw · sent by client" `figcaption`) on the left, a rose arrow (`.f3arrow`), then a fanned pile of ORA output cards (`.f3stack`, `.fs` images with `.c1/.c2/.c3` rotations) with a "{n} ORA outputs" `.f3badge` and a "Click to view all" `.f3hint`. Defaults to the first page (`subShow(0)`). One product category per page (real images from the "Jewellry" folder): page 1 Ring, page 2 Earrings, page 3 Necklace, page 4 Bracelet — each a raw factory/counter photo + 8 finished shots (studio + lifestyle + luxury editorials). Assets are root-absolute `/assets/jwl-{ring,earring,necklace,bracelet}-raw.jpg` + `-01..08.jpg` (the jewellery deck now serves from `/jewellery/`, so absolute paths). (The earlier smaller sets `bangle-*`, `earring-*`, `necklace-*`, `ring-*` are no longer used on slide 3 but are KEPT because the cover `JEWEL_COLS` wall still references them.) The `.f3row/.f3raw/.f3stack/…` CSS and the `#lightbox` wiring (`.f3page` group recognition, `.f3raw img` + `.f3stack` click handlers) live in `index.html` and are INHERITED by the furniture and fashion decks when the build scripts copy it (so `fill_slide3.py` no longer injects them). Clicking the raw photo or the stack opens the fullscreen `#lightbox` cycling that page's group (raw + 8 outputs, via `img.closest('.f3page')`); arrows/Prev/Next cycle, Esc or backdrop/image click closes. The lightbox keydown handler is registered in the capture phase and calls `stopPropagation()` so deck ArrowLeft/Right nav does not fire while it is open. (The old 5-column `.bagrid`/`.stile` layout is gone; its CSS is dead but left in place.)
4. AI generation is easy. Reliable production is not. — `.cmp` comparison table: all cells left-aligned, header row highlighted with a light band (ORALAB header keeps its green `.ora` tint), no footnote.
5. We specialise in Jewellery. — a `.jstrip` row of 4 category images (spec-ring, spec-bangle, spec-necklace, spec-rings) spanning the full width above the 4 `.card` text boxes; `.jtile img` are wired into the `#lightbox` (cycle within the strip).
6. 5 steps from raw image to final campaign, powered by the ORA Agent.
7. One platform for all your creative needs. — three cards (Social Media, Website, Marketplaces), each with a `.lgrid` logo collage of `.lgt` tiles above the copy. Social Media and Website use inline monochrome simple-icons brand glyphs in 2x2 grids (Instagram/Meta/YouTube/Pinterest; Shopify/WooCommerce/Wix/Webflow) with tag pills beneath; Marketplaces uses a self-labeled wordmark wall (Amazon, Flipkart, Myntra, Nykaa, Etsy, Walmart, Noon, Farfetch) as a 4-col x 2-row grid (`.lgrid.m4`) and no duplicate tags. All `.lgt` tiles share one fixed height so the three grids are the same height; cards are `.ucard` flex columns with `.utags` pinned to the bottom (`margin-top:auto`), so grids, descriptions and tag rows line up across the three cards. The old `[Image]` placeholders and the `oralab-diamond.mp4` preview were removed (that mp4 is now unused but kept in `assets/`). Marketplace brand logos are text because they are not in the open-source simple-icons set; swap in real logo files if provided.
8. Global brands trust us for their most important visuals. — 5 client-logo cards (Damas, Jawhara, Alliel, Carat Story, VBJ) in a centered flex-wrap (3+2 rows) with enlarged logos; no footnote.
Client work (tag "03 · Client work"; heading "Work ORA shipped for real brands.") — a 2-page `.sub2` client-work carousel, MOVED to sit right after the product carousel (it is now the 3rd content slide, `slides[3]`; the visible tags were renumbered so they stay ascending 01-10). Behaviour: every tile is PAUSED by default (first frame + `.paused` play-button overlay, no autoplay); clicking a tile opens the fullscreen `#vlb` video lightbox which plays that video with controls and has prev/next (`vlbStep`) to move through all `.sub2 video` in order, Esc/backdrop/✕ to close. `cvidSync()` is just "pause every `.sub2 video`" (called from `show()`/`sub2Show()` on slide/page change; the client-work slide index is `slides[3]`). This behaviour is shared across decks (in `index.html`). Jewellery videos: damas-campaign, jawhara-campaign, alliel-social, vbj-bridal (page 1) + keemti-1/3/4 and hillside-campaign (page 2).
10. Two ways to work with us. (was "Bring us the brief, or bring your team.") — two `.wway` vertical cards: ORA Studio (Managed Studio) and ORA Platform (Self-serve), each a big `.wlogo` mark + `.wtitle` over smaller `.wbody` copy with a bottom-anchored "Best for" line.
11. About ORA. (was "Operators and researchers, not tourists in AI.", now the subhead) — 4 founder cards, then a bottom row split into two rose-labelled groups: "Offices" (HQ · Gurgaon, Mumbai, Bangalore, London) and "Powered by" (Amazon, NVIDIA, Google, Startup India).
12. Next steps. (closing) — two cards, "Try ORA Platform" (Take a live demo and trial access for you and your team) and "Try ORA Studio" (Share a few product photos and get finished, quality-checked imagery back), then a rose `.cta` "Platform Walkthrough" button, and a `.s12foot` footer pinned to the slide bottom with the website (oralab.ai) and email (hello@oralab.ai). The CTA href is a placeholder (`https://oralab.ai`) - point it at the real walkthrough URL when there is one. Note: `.s12foot` needs the `.slide .s12foot` selector so its `position:absolute` beats the deck's `.slide>*:not(.blob){position:relative}` rule.

## Style guide (hard rules)

- No em dashes anywhere in copy.
- Headings stay white (`--ink: #FFFFFF`). Rose (`--rose: #E84E7E`) is the only accent colour for emphasis.
- Palette variables in `:root`: `--black #090909`, `--platinum #D8D8D2`, `--surface #161616`, `--border #242424`, plus `--grad-brand` (gold → pink → violet → blue) already defined; do not invent new colours.

## Content rules (agreed with the client; do not undo)

- Jewellery-only positioning. No non-jewellery examples anywhere.
- Never present stock or generic AI footage as client work. The client-work slide contains only real client deliverables.
- Say "100% checked", never "100% accurate".

## Class-name bug to avoid

The main deck JS collects slides with `document.querySelectorAll('.slide')`. Pages inside the nested carousels must NOT use `class="slide"`:

- Slide 3 sub-carousel pages use `.bap`, toggled with `.on`
- Slide 9 client-work pages use `.sub2`, toggled with `.on`

Giving an inner page `class="slide"` silently adds it to the main deck's slide list and breaks the count, dots, and arrow navigation.

Related trap: two places hardcode parent-slide indices in the ArrowUp/ArrowDown handlers: `slides[2]` for the `.bap` product carousel (slide 3) and `slides[3]` for the `.sub2` client-work carousel (now the 4th slide, moved to sit right after slide 3). If slides are added, removed, or reordered, update both indices.

## Video compression (ffmpeg)

⚠ Reconstructed default; the original session's exact settings were lost. Current assets are 1–9 MB each, so match that budget. Starting point:

```
ffmpeg -i input.mp4 -vf "scale=-2:720" -c:v libx264 -crf 28 -preset slow -pix_fmt yuv420p -an -movflags +faststart output.mp4
```

ffmpeg IS installed now (`/opt/homebrew/bin/ffmpeg`). The fashion slide-3 videos were compressed with `ffmpeg -i in -vf "scale=-2:720" -c:v libx264 -crf 28 -preset veryfast -pix_fmt yuv420p -c:a aac -b:a 96k -movflags +faststart out.mp4` (128MB of source clips → ~4.5MB total), and posters grabbed with `ffmpeg -ss 1 -i in.mp4 -vframes 1 -q:v 3 out.jpg`.

## Outstanding tasks

- Add Keemti's logo (carried over from the handoff)
- ⚠ Other outstanding items from the original session were lost; restore them from the original conversation if needed

## Workflow

- Always auto commit and push to `main` after each meaningful change, without asking for confirmation; AWS Amplify (and Netlify, if still connected) redeploys the live link automatically.
- Remote is HTTPS with the macOS keychain credential helper; git user is `collaborate-bhavishya`.
