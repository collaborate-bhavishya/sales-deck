version: 1
frontend:
  phases:
    build:
      commands:
        # Static site: no compile. Assemble a clean output folder with just the deck.
        - mkdir -p dist
        - cp -r index.html assets dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
