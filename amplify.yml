version: 1
frontend:
  phases:
    build:
      commands:
        # Static site: no compile. Create dist/ and copy just the deck into it.
        - mkdir -p dist && cp -r index.html furniture.html assets dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
